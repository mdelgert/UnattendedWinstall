# Update winget
$storeUrl = "ms-windows-store://pdp/?productid=9nblggh4nns1"

# Open the URL in the default app handler (Microsoft Store)
Start-Process $storeUrl

# Simulate a successful run (ensure this code runs successfully)
$true  # or any valid code

# If no errors occurred, set a successful exit code
exit 0